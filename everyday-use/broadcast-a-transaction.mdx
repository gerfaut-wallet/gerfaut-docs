---
title: "Broadcast"
description: "Hand a transaction signed on your signing device to the bitcoin network from Gerfaut, read what it does before it goes out, and follow it to confirmation."
---

Send a transaction your signing device signed to the network, through your backend. Gerfaut reads it back in plain words before anything leaves.

<Warning>
Nobody can take a transaction back once the network has it. Check the outputs and the fee on the preview before you confirm.
</Warning>

## Broadcast the transaction

<Steps>
  <Step title="Open the Broadcast page">
    <Tabs>
      <Tab title="Desktop">
        Select **Broadcast** in the sidebar.

        <Frame caption="Broadcasting starts from a transaction signed elsewhere. Gerfaut previews it and never signs.">
          <img src="/images/desktop/broadcast.webp" alt="The Broadcast page on desktop, empty: the Signed transaction field, the accepted formats, the Import a file and Scan a QR code buttons and the Preview button." />
        </Frame>
      </Tab>
      <Tab title="Android">
        Select **More**, then **Broadcast**.

        <Frame caption="Broadcast takes a finished transaction and hands it to the network without ever signing.">
          <img src="/images/android/broadcast.webp" alt="The Broadcast screen on Android, empty: the Signed transaction or PSBT field, the Import a file and Scan a QR code buttons and the Preview button." width="360" />
        </Frame>
      </Tab>
    </Tabs>
  </Step>
  <Step title="Paste, import or scan the transaction">
    Paste a PSBT (base64 or hex) or a raw transaction (hex), then select **Preview**. **Import a file** takes a `.psbt`, `.txn` or `.tx` file. **Scan a QR code** takes a `crypto-psbt` UR or a BBQr code.
  </Step>
  <Step title="Read the preview">
    Check the outputs, the fee and the cautions under `Before you send`. The pill reads `Ready to broadcast` when the transaction is fully signed. When the fee reads `as the PSBT claims`, no backend confirmed the coins it spends, so check the fee on your signing device.
  </Step>
  <Step title="Select Broadcast twice">
    Select **Broadcast**, read the confirmation and select **Broadcast** again. A status card shows the transaction id and the backend that took it.
  </Step>
</Steps>

<AccordionGroup>
  <Accordion title="Read the cautions under Before you send">
    None of them blocks the broadcast:

    - **No signature:** An input has no signature. Send the transaction back to the signer.
    - **Input spent or unknown:** The backend already saw the input spent, or does not know it.
    - **Fee rate:** Far above what the chain asks for.
    - **Fee:** Above 10% of the inputs.
    - **Dust:** An output below 546 sats.
    - **Locktime:** The network has not reached it yet.
  </Accordion>
  <Accordion title="Read a refusal by the node">
    The preview reads `The network refused this transaction.` followed by the node's own words.
  </Accordion>
</AccordionGroup>

## Follow the transaction

The status card checks the transaction every 30 seconds until it confirms:

- **`Waiting to be mined`:** The transaction is in the backend's mempool. Desktop reads `In the mempool`.
- **`Confirmed`:** Mined, with the number of confirmations.
- **`Not seen`:** The backend does not have it. Broadcasting it again does no harm.

Select **Check** to ask the backend now. Gerfaut lists the last ten broadcasts of each network under `Past broadcasts` on desktop and `Recent broadcasts` on Android. **Forget** removes one from that list and leaves the transaction untouched.
