---
title: "Channels"
description: "Send Gerfaut Premium alerts to the ntfy app, Telegram, an e-mail address or a webhook you run, test each channel, and verify a webhook signature."
---

Add up to ten channels per Premium licence key in **Settings** > **Premium**, under `Channels`. Gerfaut sends a test through the first one you add.

<Tip>
An ntfy, Telegram or e-mail alert names the wallet and says what happened, never an address, an amount or a transaction id. A webhook carries the whole event.
</Tip>

## Add an ntfy channel

<Steps>
  <Step title="Select Add a channel, then ntfy">
    Gerfaut draws a random topic of 24 characters. Anyone who knows the topic can read the alerts, so keep it to yourself.
  </Step>
  <Step title="Select Open in ntfy">
    The ntfy app adds the subscription. Without the app on this device, select **Copy** and subscribe to the address in the ntfy app. The line then reads `Linked`.
  </Step>
</Steps>

## Add a Telegram channel

<Steps>
  <Step title="Select Add a channel, then Telegram">
    Gerfaut shows a link code for `@GerfautAlertsBot`, good for one hour.
  </Step>
  <Step title="Select Open Telegram and press Start">
    Telegram sends the code for you. The line then reads `Linked` with the name of your chat.
  </Step>
</Steps>

## Add an e-mail channel

<Steps>
  <Step title="Select Add a channel, then E-mail">
    Enter the address, then select **Add channel** on desktop or **Add e-mail** on Android. The address receives a six-digit code, and nothing else until you confirm it.
  </Step>
  <Step title="Enter the code and select Confirm">
    Type the code in `Code` within an hour. The line then reads `Linked`. After five wrong codes, add the channel again for a new one.
  </Step>
</Steps>

## Add a webhook channel

<Note>
The host of a webhook must be, and resolve to, a public address. The server refuses a private address, such as `192.168.1.10` on your home network or `100.64.0.1` on a tailnet.
</Note>

<Steps>
  <Step title="Select Add a channel, then Webhook">
    Enter the `https://` URL of your receiver and, for signed deliveries, a `Secret`. Select **Add channel** on desktop or **Add webhook** on Android.
  </Step>
  <Step title="Receive the deliveries">
    The server posts one JSON body per event. The line reads `Linked`.

    ```json
    {
      "kind": "spend_detected",
      "wallet": "<the wallet id>",
      "wallet_name": "Cold storage",
      "at": 1789000000,
      "data": { "txid": "<the transaction id>", "value_sats": 31000, "height": null }
    }
    ```
  </Step>
</Steps>

<AccordionGroup>
  <Accordion title="Read the kind of an event">
    The `kind` is `spend_detected`, `spend_confirmed`, `coins_gone`, `receive_detected`, `receive_confirmed`, `timelock_due`, `wallet_registered` or `wallet_refused`. A `timelock_due` event carries a `milestone` of `30d`, `7d`, `1d` or `open`. A `wallet_refused` event carries the server's sentence in `message`. The account events `device_connected`, `device_approved` and `key_changed` carry `null` in `wallet` and `wallet_name`, and the `device` and its `platform` in `data`.
  </Accordion>
  <Accordion title="Verify the signature of a delivery">
    With a secret, each delivery carries `X-Gerfaut-Timestamp`, in unix seconds, and `X-Gerfaut-Signature`, which is `sha256=` followed by the hex HMAC-SHA256 of `<timestamp>.<body>` under your secret:

    ```python
    import hmac, hashlib

    expected = hmac.new(secret, f"{timestamp}.{body}".encode(), hashlib.sha256).hexdigest()
    valid = hmac.compare_digest("sha256=" + expected, signature_header)
    ```
  </Accordion>
  <Accordion title="Receive alerts on a machine without a public name">
    Use ntfy instead of a webhook. Your machine then subscribes to a topic rather than waiting to be called.
  </Accordion>
</AccordionGroup>

## Test or remove a channel

Select the menu of a channel line, then **Send a test** or **Remove**. Removing a channel asks you to [confirm with your app lock](/premium/devices#confirm-sensitive-actions). Tests and confirmation e-mails share a limit of five an hour per licence key.

<Note>
A line that reads `Not delivering` is a channel the server turned off, such as a webhook it cannot reach. Remove it and add a working one.
</Note>

<Frame caption="Every channel sits under Channels, with a menu to test it or remove it.">
  <img src="/images/android/settings-premium.webp" alt="The Premium section on Android with the Watched wallets, Channels and Recent alerts cards." width="360" />
</Frame>
