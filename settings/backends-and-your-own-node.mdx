---
title: "Backend"
description: "Pick the network Gerfaut shows and where it reads the chain: the public servers, your own Esplora endpoint or your own Electrum server."
---

Point Gerfaut at your own node to keep your addresses to yourself, or stay on the public servers. Each network keeps its own backend.

<Tabs>
  <Tab title="Desktop">Select **Settings** at the bottom of the sidebar, then **Network**.</Tab>
  <Tab title="Android">Select **More**, then **Settings**, then **Network**.</Tab>
</Tabs>

## Pick the network

In the `Network` card, select `Mainnet`, `Signet`, `Testnet 4` or `Regtest`. Gerfaut shows the wallets of that network only, and names it in a badge unless it is mainnet. Regtest has no public server, so point it at your own node.

## Use the public servers

<Warning>
The operator of the public server that answers sees the addresses of your wallets.
</Warning>

The `Backend` card opens on `Public API`, with nothing to set up. Under `Server`, keep `Automatic` to let Gerfaut try the public servers of the network in turn, or select one operator, such as `mempool.space`, to ask only that one.

<Tabs>
  <Tab title="Desktop">
    <Frame caption="Each network keeps its own backend: a public server, or an Esplora or Electrum instance on your own node.">
      <img src="/images/desktop/settings-node.webp" alt="The Network section of the desktop settings: the network picker with Signet selected, the Backend card with Public API and blockstream.info as the server, My own Esplora, My own Electrum server and Save backend." />
    </Frame>
  </Tab>
  <Tab title="Android">
    <Frame caption="Each network keeps its own backend: a public server, or an Esplora or Electrum instance on your own node.">
      <img src="/images/android/settings-node.webp" alt="The Network section on Android: the network picker with Signet selected and the Backend card with Public API, My own Esplora, My own Electrum server, the Server list on Automatic and Save backend." width="360" />
    </Frame>
  </Tab>
</Tabs>

## Connect your own Esplora

<Steps>
  <Step title="Select My own Esplora">
    The `Server URL` field appears, for an address such as `https://node.example.org:3002/api`.
  </Step>
  <Step title="Enter the URL, or scan it">
    Select **Scan** beside the field to read the QR code your node shows for its Esplora app.
  </Step>
  <Step title="Select Save backend">
    Gerfaut uses it from the next sync.
  </Step>
</Steps>

## Connect your own Electrum server

<Warning>
Leave `TLS` on. Without it, anyone on the path between your device and the server reads the addresses you watch.
</Warning>

<Steps>
  <Step title="Select My own Electrum server">
    Use it for electrs or Fulcrum.
  </Step>
  <Step title="Enter the host and the port">
    Enter the `Host`, such as `node.example.org` or a `.onion` address reached [through Tor](/settings/tor), and the `Port`, `50002` by default. **Scan** reads the QR code your node shows for its Electrum app and fills both.
  </Step>
  <Step title="Select Save backend">
    A server that signs its own certificate shows it to you before Gerfaut saves anything. See [Certificates](/settings/certificates).
  </Step>
</Steps>

<Note>
An Electrum server cannot serve a wallet [added from a single address](/wallets/watch-an-address). Keep such a wallet on a network whose backend is `Public API` or your own Esplora.
</Note>
