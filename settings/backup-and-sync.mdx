---
title: "Backup and sync"
description: "Save every wallet you watch in an encrypted Gerfaut backup file or an animated QR code, and restore it on another computer or phone."
---

Seal every wallet you watch in a backup under a password of your own, as a file or an animated QR code, and restore it on another computer or phone. A backup holds descriptors, addresses and names, never a private key or a seed.

Open **Settings** > **Backup & sync**.

<Tabs>
  <Tab title="Desktop">
    <Frame caption="A backup carries descriptors and addresses under a password, never a private key or a seed, and moves wallets between desktop and phone.">
      <img src="/images/desktop/settings-backup.webp" alt="The Backup & sync section of the desktop settings: the explanation and the Export and Restore buttons." />
    </Frame>
  </Tab>
  <Tab title="Android">
    <Frame caption="A backup carries descriptors and addresses under a password, never a private key or a seed.">
      <img src="/images/android/settings-backup.webp" alt="The Backup & sync section on Android with the explanation and the Export and Restore buttons." width="360" />
    </Frame>
  </Tab>
</Tabs>

## Export a backup

<Warning>
Nobody can recover the password. Someone who copies the file can try passwords offline, so pick a long passphrase of several words.
</Warning>

<Steps>
  <Step title="Select Export…">
    The dialog `Export a backup` opens.
  </Step>
  <Step title="Pick what goes in">
    Keep `All wallets` or take only the wallets of the selected network. Tick `Include node settings` to add your backend, the certificates you accepted and the gap limit.
  </Step>
  <Step title="Pick a password">
    Enter it in `Password` and `Confirm password`, then select **Create backup**.
  </Step>
  <Step title="Save the file or show the QR code">
    Select **Save file** to write a `.gerfaut` file, or **Share** on Android. Select **Show QR code** to show the backup as an animated QR code.
  </Step>
</Steps>

## Restore a backup

<Steps>
  <Step title="Select Restore…">
    The dialog `Restore a backup` opens.
  </Step>
  <Step title="Open the backup">
    Select **Open a file** and pick the `.gerfaut` file, or **Scan a QR code** and keep the camera on the code until the counter reaches its total. One loop of the code is enough.
  </Step>
  <Step title="Enter the password">
    Select **Open backup**. A wrong password reads `Wrong password, or the file is damaged.`
  </Step>
  <Step title="Pick what to restore">
    Gerfaut ticks every wallet this device does not watch yet, and marks the others `Already watched`. Tick `Apply node settings` to replace your backend settings with those of the backup.
  </Step>
  <Step title="Select Restore">
    Gerfaut adds the wallets and syncs them.
  </Step>
</Steps>

<Note>
On Android, the encryption key of the vault never leaves the phone, so a system backup of the phone does not bring your wallets back. Keep a Gerfaut backup for a new phone.
</Note>
