---
title: "Backup and sync"
description: "Save every wallet you watch in an encrypted Gerfaut backup file or an animated QR code, and restore it on another computer or phone."
---

A backup holds every wallet you watch, sealed with a password you choose, as a file or as an animated QR code. The same backup restores on another device, desktop or phone: this is how your wallets travel, with no server in between.

A backup holds descriptors, addresses, names and, if you ask, your node settings. It never holds a key, and it never holds chain data: the other device syncs on its own.

Open **Settings** > **Backup & sync**.

<Tabs>
  <Tab title="Desktop">
    <Frame caption="A backup carries descriptors and addresses under a password, never a key, and moves wallets between desktop and phone.">
      <img src="/images/desktop/settings-backup.webp" alt="The Backup & sync section of the desktop settings: the explanation and the Export and Restore buttons." />
    </Frame>
  </Tab>
  <Tab title="Android">
    <Frame caption="A backup carries descriptors and addresses, encrypted with a password, never a key.">
      <img src="/images/android/settings-backup.webp" alt="The Backup & sync section on Android with the explanation and the Export and Restore buttons." width="360" />
    </Frame>
  </Tab>
</Tabs>

## Export a backup

<Steps>
  <Step title="Select Export…">
    The dialog `Export a backup` opens.
  </Step>
  <Step title="Choose what goes in">
    Under `Wallets`, keep `All wallets` or take only the wallets of the selected network. Select `Include node settings` to add your backend choice, the certificates you accepted and the gap limit.
  </Step>
  <Step title="Choose a password">
    Enter it in `Password` and `Confirm password`, 8 characters at least, then select **Create backup**. The summary reads the number of wallets and the size of the backup.
  </Step>
  <Step title="Save the file or show the QR code">
    Select **Save file** to write `gerfaut-backup-YYYY-MM-DD.gerfaut` where you want it. On Android, **Share** hands the file to another app. Select **Show QR code** to display the backup as an animated QR code for the other device, see below.
  </Step>
</Steps>

<Note>
There is no way to recover the password: you type it on the other device. The file can be copied and guessed offline, so use a long passphrase of several words.
</Note>

## Restore a backup

<Steps>
  <Step title="Select Restore…">
    The dialog `Restore a backup` opens.
  </Step>
  <Step title="Open the backup">
    Select **Open a file** and pick the `.gerfaut` file, or **Scan a QR code** and keep the camera on the animated code until the counter reaches its total. The dialog then reads `Backup read from` the file or the QR code.
  </Step>
  <Step title="Enter the password">
    Select **Open backup**. A wrong password reads `Wrong password, or the file is damaged.`
  </Step>
  <Step title="Pick what to restore">
    Each wallet of the backup is listed with its network and its kind, ticked unless this device already watches it, in which case it reads `Already watched`. When the backup carries node settings, select `Apply node settings` to replace yours with them; the dialog lists what would change. Then select **Restore**.
  </Step>
</Steps>

Gerfaut switches to the network of the first restored wallet if none is on the selected one, then syncs.

## Move your wallets between two devices

<Steps>
  <Step title="Show the QR code on the first device">
    Export a backup and select **Show QR code**. The code loops through its frames every 200 milliseconds, with a counter such as `3 / 8`, and stays on screen until you select **Done**.
  </Step>
  <Step title="Scan it on the second device">
    Open **Settings** > **Backup & sync** > **Restore…**, select **Scan a QR code** and keep the camera on the code. It can start at any frame: one loop is enough.
  </Step>
  <Step title="Enter the password and restore">
    Type the password you chose on the first device and select **Restore**.
  </Step>
</Steps>

<Note>
On Android, the key that opens the vault lives in the phone's secure storage and never travels: a phone restored from a system backup comes back with the vault file and without the key. A Gerfaut backup is what brings the wallets back on a new phone.
</Note>
